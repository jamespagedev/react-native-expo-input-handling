export const shopApiEndpoints: { [key: string]: string } = {
  shops: "shops",
};

export const productsApiEndpoints: { [key: string]: string } = {
  products: "products",
};

export const dwarfmartApiEndpoints: { [key: string]: string } = {
  products: "products",
};

export const hitormissApiEndpoints: { [key: string]: string } = {
  products: "products",
};

export const cybermonkeysApiEndpoints: { [key: string]: string } = {
  products: "products",
};

export const screenNavigations: {
  [key: string]: { route: string; screenTitle: string };
} = {
  products: { route: "products", screenTitle: "New Order" },
  review: { route: "review", screenTitle: "New Order" },
  shop: { route: "shop", screenTitle: "New Order" },
};
